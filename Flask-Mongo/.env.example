# Server Config
SERVER_PORT=3000

# MongoDB Configuration
# For MongoDB Atlas, use the <password> placeholder in the URL and provide MONGODB_PASSWORD separately
# This ensures special characters in the password are properly URL-encoded
MONGODB_URL="mongodb+srv://username:<password>@cluster.mongodb.net/?retryWrites=true&w=majority"
MONGODB_PASSWORD="your_password_with_special_chars"
MONGODB_DATABASE="your_database_name"

# Alternative: If your password is already in the URL, you can omit MONGODB_PASSWORD
# MONGODB_URL="mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority"